{
  "name": "admin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "postinstall": "npm install --prefix ../shared"
  },
  "dependencies": {
    "firebase": "^12.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
